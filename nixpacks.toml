[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[[ports]]
port = "3000"
protocol = "tcp"

[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"