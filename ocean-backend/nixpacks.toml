[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd .medusa/server && npm install --omit=dev && npx medusa db:migrate && node index.js"

[variables]
NODE_ENV = "production"
