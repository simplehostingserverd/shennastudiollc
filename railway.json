{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd ocean-backend && npm install && cd .. && npm run build && cd ocean-backend && npx --yes @medusajs/cli@latest build"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "./railway-start.sh"
  }
}
